
<nav class="navbar navbar-expand-lg navbar-dark  bg-primary">
  <div class="container">
    <a class="navbar-brand" href="#"><%= ENV['COMPANY'] %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>
        
      </ul>
    
      <div class="d-flex">
        
        
        <% if logged_in? %> 
            <%= link_to "Profile", user_path(current_user.id), class: 'btn text-light' %> 
            <%= link_to "Logout", session_path(current_user.id), method: :delete, class: 'btn text-light' %> 
            
            
        <% else %> 
            <%= link_to "Login", login_path, class: 'btn text-light'  %> 
           <%= link_to "Sign up", register_path, class: 'btn btn-light ms-4' %> 
        <% end %> 
      </div>
    </div>
  </div>
</nav>